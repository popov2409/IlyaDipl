<UserControl x:Class="IlyaDipl.View.TtriangleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IlyaDipl.View"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="80" PreviewMouseDown="Base_MouseDown" MouseMove="Base_MouseMove" HorizontalAlignment="Left" VerticalAlignment="Top">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Name="DeleteElement" Header="Удалить" Click="DeleteElement_OnClick"/>
        </ContextMenu>
    </UserControl.ContextMenu>
    <Grid Background="Transparent" Name="BaseGrid">
        <TextBlock Name="NameTextBlock" HorizontalAlignment="Right" VerticalAlignment="Center" TextWrapping="Wrap"  TextAlignment="Right" Panel.ZIndex="2" Margin="0,0,6,0" FontSize="10"/>
    </Grid>
</UserControl>
